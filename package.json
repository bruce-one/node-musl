{
  "name": "node-musl",
  "version": "0.0.5",
  "description": "",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/bruce-one/node-musl.git"
  },
  "os": [
    "linux"
  ],
  "cpu": [
    "x64",
    "ia32"
  ],
  "directories": {
    "test": "test"
  },
  "bin": {
    "musl-gcc": "./bin/musl-gcc",
    "musl-g++": "./bin/musl-g++",
    "musl-ld": "./bin/musl-ld",
    "musl-exports": "./bin/exports.js",
    "musl-env": "./bin/env.js"
  },
  "scripts": {
    "clean": "node-pre-gyp clean && node-gyp clean && rm -rf bootstrap",
    "test": "node test/test.js",
    "install": "node-pre-gyp install --fallback-to-build --target_platform=hack --target_arch=${BUILD_ARCH:-$(node -p process.arch)}",
    "postinstall": "node symlinks.js ${BUILD_ARCH:-$(node -p process.arch)}"
  },
  "author": {
    "name": "Bryce Gibson"
  },
  "binary": {
    "module_name": "node_musl",
    "module_path": "{arch}-linux-musl",
    "host": "https://github.com/bruce-one/node-musl/releases/download/",
    "remote_path": "{version}",
    "package_name": "{module_name}-v{version}-{arch}.tar.gz"
  },
  "license": "ISC",
  "gypfile": true,
  "dependencies": {
    "debug": "^2.6.1",
    "klaw-sync": "^1.1.2",
    "node-pre-gyp": "^0.6.33"
  },
  "devDependencies": {
    "node-pre-gyp-github": "^1.3.1"
  }
}
